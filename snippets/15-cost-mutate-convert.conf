input { stdin { } }

filter {
  mutate {
    rename => { "message" => "raw_message" }
    add_field => { "pipeline_stage" => "normalized" }
    convert => { "status" => "integer" "latency_ms" => "integer" }
  }
}

output { stdout { codec => rubydebug } }
